export const metadata = {
    title: "Edit Profile",
    description: "Update your camp alias, bio, contact details, and profile picture.",
    robots: { index: false },
};
export default function Layout({ children }) { return <>{children}</>; }
