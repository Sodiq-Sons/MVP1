import ProfilePage from "@/components/ProfilePage";
import React from "react";

const page = () => {
    return (
        <main>
            <ProfilePage />
        </main>
    );
};

export default page;
