export const metadata = {
    title: "Welcome to Camp Connect",
    description:
        "Pick your camp alias, choose your platoon, and join thousands of NYSC corps members already on Camp Connect.",
    robots: { index: false },
};

export default function OnboardingLayout({ children }) {
    return <>{children}</>;
}
