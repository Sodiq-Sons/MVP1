export const metadata = {
    title: "Camp Post",
    description: "Read and vote on this camp post — gists, issues, and polls from NYSC corps members.",
};

export default function IssueLayout({ children }) {
    return <>{children}</>;
}
