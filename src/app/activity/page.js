import ActivityPage from "@/components/ActivityPage";
import React from "react";

const page = () => {
    return (
        <main>
            <ActivityPage />
        </main>
    );
};

export default page;
